# S<N> — <question as a question>

**Status:** ⬜ Not started
**Blocking:** <yes/no, and what it blocks>
**Spawned by:** <parent spike, if this exists because another hit a blocker>
**Needs:** <local docker / a deployed server / a second player / AWS>
**Run by:**
**Date run:**
**Commit / branch:**

---

## Spec

**Question:** <the one thing this answers, restated so the title is not the only copy>

**Pass bar:** <what specifically must be true. A number or an observable, not "it works">

**This decides:** <what changes depending on the answer — name the design choice>

**Why this spike exists:** <the finding, conflict or assumption that created it>

---

## Prior art

_Not yet searched._

<!-- REQUIRED, and do this BEFORE running anything.
     Has somebody already answered this? Record the searches that found NOTHING
     as well as the hits — "no prior art" is only a finding if somebody looked.

     SAY WHETHER YOU READ IT. A search-result summary is not a source. Mark each
     citation read-in-full or skimmed, so nobody later builds on a claim nobody
     actually opened. This project has already been bitten twice: a guide about
     LOCAL servers was read as applying to dedicated ones (the -insecure error),
     and a "menu-driven" description nearly buried 13 real console commands that
     were sitting in the plugin source the whole time. -->

---

## What was actually done

_Not yet run._

<!-- Commands run, files touched, versions pinned, what was skipped and why.
     Enough that someone can re-run it and get the same result. Paste the
     commands verbatim; a described command is a command nobody can repeat. -->

---

## Result

_Not yet run._

<!-- Raw output, measurements, numbers. Paste the evidence, not a summary.
     Record invalidated measurements rather than deleting them — a wrong reading
     that got corrected is how the next person avoids the same wrong reading.
     A zero result is ambiguous between "absent" and "I measured it wrong":
     never report one as the other. -->

---

## Answer

_Not yet run._

<!-- One paragraph answering the question in the title. PASS / FAIL / PARTIAL
     against the pass bar above, and say which. "Partial" is a real answer —
     record what held and what did not. -->

---

## Consequences

_Not yet run._

<!-- What changed in the design because of this, and what it closed off.
     If this invalidates something in CLAUDE.md, an .env comment, an
     rcon-manifest note or the L4D2 dossier, say so and fix it in the same
     commit.
     If it produced a blocker rather than an answer, SPAWN A SPIKE. -->
